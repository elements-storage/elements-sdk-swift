//
// FileSizeDistributionItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct FileSizeDistributionItem: Codable, JSONEncodable, Hashable {

    public var min: Int
    public var max: Int
    public var count: Int
    public var percent: Int
    public var average: Int
    public var total: Int

    public init(min: Int, max: Int, count: Int, percent: Int, average: Int, total: Int) {
        self.min = min
        self.max = max
        self.count = count
        self.percent = percent
        self.average = average
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case min
        case max
        case count
        case percent
        case average
        case total
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(min, forKey: .min)
        try container.encode(max, forKey: .max)
        try container.encode(count, forKey: .count)
        try container.encode(percent, forKey: .percent)
        try container.encode(average, forKey: .average)
        try container.encode(total, forKey: .total)
    }
}

