//
// AlertUpdate.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct AlertUpdate: Codable, JSONEncodable, Hashable {

    public var name: String
    public var message: String
    public var level: String
    public var isOpen: Bool
    public var closedAt: Date?

    public init(name: String, message: String, level: String, isOpen: Bool, closedAt: Date? = nil) {
        self.name = name
        self.message = message
        self.level = level
        self.isOpen = isOpen
        self.closedAt = closedAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case message
        case level
        case isOpen = "is_open"
        case closedAt = "closed_at"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(message, forKey: .message)
        try container.encode(level, forKey: .level)
        try container.encode(isOpen, forKey: .isOpen)
        try container.encodeIfPresent(closedAt, forKey: .closedAt)
    }
}

